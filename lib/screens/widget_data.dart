import 'package:calc/widget/button.dart';
import 'package:calc/widget/cal_button.dart';
import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

List<Widget> buttonList = [
  const button(label: 'AC'),
  const button(label: 'C'),
  const button(label: '^'),
  const button(label: '÷'),
  const button(label: '7'),
  const button(label: '8'),
  const button(label: '9'),
  const button(label: '×'),
  const button(label: '4'),
  const button(label: '5'),
  const button(label: '6'),
  const button(label: '-'),
  const button(label: '1'),
  const button(label: '2'),
  const button(label: '3'),
  const button(label: '+'),
  const button(label: '0'),
  const button(label: '.'),
  const cal_button(label: '='),
];
